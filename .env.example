# database url
DATABASE_URL='postgresql://neondb_owner:npg_2w4ySTmAjxGZ@ep-restless-mud-adv55uwr-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'

# fake bank api url
# Endpoint used by `createOnRampTxn` to initiate payments (fake bank server)
BANK_API_URL='http://localhost:4001/initiatePayment'

# bank webhook url
# Where the bank posts transaction callbacks. Handled by `apps/bank-webhook`.
BANK_WEBHOOK_URL='http://localhost:3003/bankWebhook'

# user app url
USER_APP_URL='http://localhost:3000'

# NextAuth (used by `apps/user-app`)
# Generate a long random secret for production. Example: `openssl rand -base64 32`
NEXTAUTH_SECRET='change_this_to_a_secure_random_string'
NEXTAUTH_URL='http://localhost:3000'

# Server ports (you can override per-process if needed)
# Default port used by services that read `PORT`.
PORT=3003
# Fake bank service default port (not referenced directly in code but useful to document)
FAKE_BANK_PORT=4001

# P2P retries
P2P_MAX_RETRIES=4